use anyhow::Result;

fn main() {
    pulumi_gestalt_rust::run_with_packages({{PACKAGES}}, pulumi_main).unwrap();
}

fn pulumi_main(ctx: &pulumi_gestalt_rust::Context) -> Result<()> {
    {{CONTENT}}
    Ok(())
}
